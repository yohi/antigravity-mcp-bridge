The dispatch code `packages/extension/src/handlers.ts` successfully implements sending text and payload model.
The changes from the previous conversation are preserved.

The user should now:
1. Reload the window using "Developer: Reload Window".
2. Execute a dispatch prompt using the new models setup.
